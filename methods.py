#методы списков
#list.append(x),list.extend(x), list.sort(key=None, reverse=False), list.reverse(x), list.insert(i,x)

#методы строк
#str.replace(old, new[, count]),str.split(sep=None, maxsplit=-1),str.title(),str.upper(), str.lower()

#методы словарей
#dict.items(),dict.keys(), dict.values(), dict.copy()

#методы множеств
# set.add(a),set.difference(x), set.intersection(x),set.issubset(x), set.remove()